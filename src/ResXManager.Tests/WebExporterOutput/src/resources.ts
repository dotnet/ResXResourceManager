/*
 * This code was generated by ResXResourceManager.
 * Changes to this file may cause incorrect behavior and will be lost if the code is regenerated.
 */

/* eslint-disable */

function formatString(template: string, args: any): string {
  let result = template;
  Object.entries(args).forEach(entry => {
    const key = entry[0];
    const replacement = `${entry[1]}`;
    const pattern = new RegExp('\\$\\{\\s?' + key + '\\s?\\}|\\{\\{\\s?' + key + '\\s?\\}\\}', 'gm');
    result = result.replace(pattern, replacement);
  });
  return result;
}

// noinspection JSUnusedGlobalSymbols,SpellCheckingInspection
export class WebExportTest {
  SimpleString = "Simple String";
  private WithInterpolation_TEMPLATE = "Voltage: {{ voltage }}, Current: ${current}.";
  WithInterpolation = (args: { voltage: string | number, current: string | number }) => {
    return formatString(this.WithInterpolation_TEMPLATE, args);
  }
  private WithBadInterpolation_TEMPLATE = "Voltage: {{ voltage }}, Current: ${current}.";
  WithBadInterpolation = (args: { voltage: string | number, current: string | number }) => {
    return formatString(this.WithBadInterpolation_TEMPLATE, args);
  }
}

