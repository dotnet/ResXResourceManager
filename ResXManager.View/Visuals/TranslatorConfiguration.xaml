<UserControl x:Class="tomenglertde.ResXManager.View.Visuals.TranslatorConfiguration"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" d:DesignHeight="100" d:DesignWidth="500"
             xmlns:translators="clr-namespace:tomenglertde.ResXManager.Translators;assembly=ResXManager.Translators"
             xmlns:wpf="urn:TomsToolbox"
             >
  <TabControl TabStripPlacement="Left" ItemsSource="{Binding Source={x:Static translators:TranslatorHost.Translators}}" DisplayMemberPath="DisplayName">
    <TabControl.ContentTemplate>
      <DataTemplate DataType="translators:ITranslator">
        <StackPanel Margin="5">
          <CheckBox Content="Enabled" IsChecked="{Binding IsEnabled}" />
          <Decorator Height="5"/>
          <GroupBox Header="Authentication" Grid.IsSharedSizeScope="True">
            <StackPanel>
              <ItemsControl ItemsSource="{Binding Credentials}" Margin="2" >
                <ItemsControl.ItemTemplate>
                  <DataTemplate DataType="translators:ICredentialItem">
                    <wpf:SharedWidthHeaderedContentControl Margin="2">
                      <wpf:SharedWidthHeaderedContentControl.Header>
                        <TextBlock DockPanel.Dock="Left"><Run Text="{Binding Description, Mode=OneWay}"/><Run Text=": "/></TextBlock>
                      </wpf:SharedWidthHeaderedContentControl.Header>
                      <TextBox Text="{Binding Value}" />
                    </wpf:SharedWidthHeaderedContentControl>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
              <wpf:SharedWidthHeaderedContentControl Margin="2">
                <CheckBox Content="Save credentials" IsChecked="{Binding SaveCredentials}" ToolTip="Credentials will be saved as clear text in your local profile!"/>
              </wpf:SharedWidthHeaderedContentControl>
            </StackPanel>
          </GroupBox>
        </StackPanel>
      </DataTemplate>
    </TabControl.ContentTemplate>
  </TabControl>
</UserControl>
